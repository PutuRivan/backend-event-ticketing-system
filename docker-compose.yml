services:
  redis:
    image: redis:8.0.1
    container_name: event-ticketing-redis
    ports:
      - "6379:6379"
    command: redis-server --save 20 1 --loglevel warning
    volumes:
      - redis-data:/data


volumes:
  redis-data: